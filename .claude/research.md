# NixOS Configuration — Deep Research Report

> Generated by Claude Code, 2026-02-28

---

## Overview

This is a **flake-based, multi-host, multi-platform NixOS + nix-darwin configuration** for Ian Preston's personal and work machines. It manages both NixOS (Linux) and macOS systems from a single declarative repository, with full secrets management, declarative disk partitioning, and a modular composition pattern throughout.

The project draws inspiration from [EmergentMind's nix-config](https://github.com/EmergentMind/nix-config) and follows that playbook for structure and patterns.

---

## Project Layout

```
nixos/
├── flake.nix              # Root: inputs, outputs, auto-discovery of hosts
├── flake.lock             # Locked dependency versions
├── lib/default.nix        # Custom Nix library (relativeToRoot, scanPaths)
├── hostSpecs/             # Per-host metadata modules (name, user, email, flags)
├── hosts/
│   ├── common/            # Shared host-level modules (core, optional, disks, users)
│   ├── nixos/             # NixOS host entry points (terra, luna, toshibachromebook, iso)
│   └── darwin/            # nix-darwin host entry points (work)
├── home/
│   ├── core/              # Shared home-manager modules (git, zsh, neovim, packages…)
│   ├── ipreston/          # Personal per-host home configs
│   └── ian.preston/       # Work per-host home configs
├── nixos-installer/       # Standalone minimal flake for bootstrap installs
├── scripts/               # Bootstrap, dconf extraction, sops helpers
├── assets/                # Wallpapers and media assets
└── Taskfile.yaml          # Task runner (rebuild, update, gc, ISO build…)
```

---

## Flake Inputs

| Input | Source | Version |
|-------|--------|---------|
| nixpkgs | nixos/nixpkgs | nixos-25.11 |
| nixpkgs-darwin | nixos/nixpkgs | nixpkgs-25.11-darwin |
| nixpkgs-stable | nixos/nixpkgs | nixos-25.11 |
| nixpkgs-unstable | nixos/nixpkgs | nixos-unstable |
| nix-darwin | lnl7/nix-darwin | nix-darwin-25.11 |
| hardware | nixos/nixos-hardware | (latest) |
| home-manager | nix-community/home-manager | release-25.11 |
| stylix | danth/stylix | release-25.11 |
| nix-flatpak | gmodena/nix-flatpak | (latest) |
| disko | nix-community/disko | (latest) |
| sops-nix | Mic92/sops-nix | (latest) |
| nix-secrets | private git+ssh repo | (latest) |

`nixpkgs-unstable` is used selectively (e.g., for OpenRGB which needs newer packages). `nix-secrets` is a private repository containing encrypted SOPS secrets (passwords, SSH keys, work-specific hostnames/usernames).

---

## Supported Platforms

- `x86_64-linux`
- `aarch64-linux`
- `x86_64-darwin`
- `aarch64-darwin`

---

## Hosts

### terra — Desktop Workstation (NixOS, x86_64)

- **CPU**: AMD
- **GPU**: NVIDIA RTX 5080 (stable drivers, modesetting enabled, power management)
- **Disk**: Single NVMe (`nvme0n1`), btrfs with subvolumes (`@root`, `@nix`, `@swap`), 32 GB swapfile
- **Filesystems**: zstd compression, noatime
- **Desktop**: GNOME + Wayland + GDM
- **Notable**: Sunshine game-streaming server; OpenRGB; ZSA keyboard firmware; Flatpak apps (Discord, Chrome, BambuStudio, Headlamp); Docker (rootless); PipeWire audio; printer support

**Home config** (`home/ipreston/terra.nix`): imports core + adb, browser (Firefox), gnome extras, calibre, comms (Signal), freecad, media (Spotify, VLC), ssh client config, sops secrets

---

### luna — Gaming Laptop (NixOS, x86_64)

- **Hardware**: MSI GS43VR-6RE Phantom Pro
- **CPU**: Intel
- **GPU**: Intel iGPU + NVIDIA GTX 1060 (PRIME offloading, older GPU workarounds)
- **Kernel**: Latest
- **Disk**: NVMe (system) + SSD (data at `/mnt/data`), btrfs, 25 GB swapfile
- **Desktop**: GNOME + Wayland
- **Notable**: Xreal Air AR headset udev rules; Moonlight game-streaming client; similar apps to terra

**Home config** (`home/ipreston/luna.nix`): imports core + browser, gnome, moonlight, comms, media, sops

---

### toshibachromebook — Chromebook Test Device (NixOS, x86_64)

- **CPU**: Intel
- **Boot**: Legacy BIOS (`cros_legacy` GRUB target)
- **Disk**: 2 MB BIOS partition + 514 MB EFI + btrfs root, 4 GB swapfile
- **Desktop**: GNOME
- **Purpose**: Minimal test/development target

---

### iso — Custom NixOS Live ISO

- Extends `nixos/modules/installer/cd-dvd/installation-cd-minimal-new-kernel-no-zfs`
- SSH server enabled (for remote installation)
- systemd-boot with `zstd:3` compression
- Home-manager active with core utilities
- Build timestamp shown in shell prompt (so you know how old the ISO is)

---

### work — Work MacBook (nix-darwin, aarch64)

- **Platform**: macOS (Apple Silicon)
- **Hostname / Username**: pulled from encrypted nix-secrets (not hardcoded)
- **Homebrew**: managed declaratively via nix-darwin
  - Taps: `hashicorp/tap`, `databricks/tap`
  - Packages: `awscli`, `azure-cli`, `node`, `uv`, `sf` (Salesforce CLI), `databricks`, `terraform`, `gcloud`
  - Casks: `ghostty`, `obsidian`
- **Git**: conditional `includeIf` for databricks-eng and databricks-field-eng GitHub orgs
  - SSH host alias `git@github.com-emu` for EMU (Enterprise Managed User) authentication
- **Shell extras**: `llm` and `isaac` aliases; env vars for internal Databricks tooling
- **Ghostty terminal**: Catppuccin Latte theme, FiraCode Nerd Font, explicit config in `~/.config/ghostty/config`

**Home config** (`home/ian.preston/work.nix`): git work overrides, Ghostty config, zsh aliases, work env vars

---

### penguin — WSL / Generic Linux (standalone home-manager)

- No NixOS system layer
- `targets.genericLinux.enable = true`
- Home-manager standalone (not as NixOS module)
- Core home config only

---

## Custom Library (`lib/default.nix`)

Two helper functions:

```nix
relativeToRoot = path: (root + "/${path}");
scanPaths = path: builtins.map (f: (path + "/${f}")) (
  builtins.attrNames (lib.filterAttrs
    (name: _type: (_type == "regular" && lib.hasSuffix ".nix" name && name != "default.nix")
                || (_type == "directory"))
    (builtins.readDir path))
);
```

`scanPaths` is used extensively to auto-import all `.nix` files in a directory (excluding `default.nix`). This avoids explicit enumeration when adding new optional modules.

---

## hostSpecs System

Each host has a corresponding module in `hostSpecs/` that declares metadata:

| Attribute | Type | Default | Notes |
|-----------|------|---------|-------|
| `hostName` | string | — | Machine hostname |
| `hostNameFile` | string | hostName | Used when filename ≠ hostname |
| `username` | string | `"ipreston"` | Overridden for work (from secrets) |
| `userFullName` | string | `"Ian Preston"` | — |
| `email.gitHub` | string | — | Personal GitHub email |
| `email.work` | string | — | Work email |
| `gh_user` | string | `"ianepreston"` | GitHub username |
| `isMinimal` | bool | false | Skip heavy packages |
| `isWork` | bool | false | Enable work-specific config |
| `isDarwin` | bool | false | macOS platform flag |
| `home` | string | computed | `/home/{username}` or `/Users/{username}` |

These are evaluated as NixOS modules and passed to all configurations via `specialArgs`. Modules gate logic on these flags (e.g. `lib.mkIf hostSpec.isWork { ... }`).

---

## Host-Level Modules

### Core (`hosts/common/core/`)

**`nixos.nix`** — Applied to all NixOS hosts:
- Firmware support, terminal database (`ncurses`)
- Nix settings: flakes + nix-command, auto-optimise-store, trusted users, registries pointing to flake inputs
- Locale: `en_CA.UTF-8`, timezone `America/Edmonton`
- Sudo: passwordless for `wheel`, 2 h timestamp, `SSH_AUTH_SOCK` preserved

**`ssh.nix`** — SSH server + client core:
- `sshd` with public-key-only auth
- SSH agent with KDE askpassword dialog
- GitHub known hosts pre-seeded (ed25519, rsa, ecdsa)

**`sops.nix`** — Secrets management:
- Shared secrets file path from nix-secrets
- SSH host key → age key derivation (automatic)
- Per-user age key support for home-manager

**`darwin.nix`** — macOS core: currently empty stub

---

### Optional (`hosts/common/optional/`)

| Module | What It Configures |
|--------|-------------------|
| `gnome.nix` | GNOME 45+, Wayland, GDM, bloatware exclusions |
| `kde.nix` | KDE Plasma 6, SDDM Wayland |
| `audio.nix` | PipeWire with ALSA + PulseAudio compatibility |
| `nvidia-rtx5080.nix` | RTX 5080, stable drivers, power mgmt, modesetting |
| `nvidia-gtx1060.nix` | GTX 1060, PRIME offload, older GPU workarounds |
| `gaming.nix` | Steam, Proton, Gamescope, Xbox controller |
| `rgb.nix` | OpenRGB with all plugins, I2C kernel modules |
| `zsa-keeb.nix` | ZSA keyboard via Keymapp |
| `xreal-headset.nix` | Xreal Air AR headset udev rules + Google Chrome |
| `sunshine.nix` | Game streaming server, firewall rules, GNOME extension |
| `flatpak.nix` | Flatpak + xdg-portal, pre-installed: Discord, BambuStudio, Chrome, Headlamp |
| `obsidian.nix` | Obsidian editor |
| `docker.nix` | Rootless Docker |
| `smbclient.nix` | CIFS mounts for NAS (laconia: media + home shares) |
| `services/printing.nix` | CUPS, cups-browsed disabled (security) |
| `themes.nix` | Stylix with Catppuccin Latte + Fira Code Nerd Font |
| `minimal-user.nix` | Temporary hashed password for minimal/ISO builds |

---

### Disk Configurations (`hosts/common/disks/`)

All configs use **disko** for declarative partitioning.

**terra.nix**: Single NVMe
```
nvme0n1
├── EFI: 512 MB vfat
└── root: btrfs
    ├── @root  → /
    ├── @nix   → /nix
    └── @swap  → swap (32 GB swapfile)
```

**luna.nix**: NVMe (system) + SSD (data)
- System NVMe: same btrfs layout as terra, 25 GB swapfile
- `/dev/sda` or secondary: mounted at `/mnt/data`

**toshibachromebook.nix**: Legacy BIOS hybrid
```
sda
├── BIOS grub: 2 MB
├── EFI: 514 MB vfat
└── root: btrfs (subvolumes), 4 GB swapfile
```

---

### Users (`hosts/common/users/primary/`)

**`nixos.nix`** — Declarative user definition:
- Shell: zsh
- `mutableUsers = false` (fully declarative)
- Password: hashed, from sops secrets
- SSH public keys: from `./keys/` directory
- Groups: `wheel audio video docker git networkmanager plugdev scanner lp render`
- systemd tmpfiles: `.ssh/` directory structure

---

## Home-Manager Configuration

### Core Modules (`home/core/`)

All users on all platforms get these:

#### `packages.nix`
CLI tools: `gh`, `ripgrep`, `fd`, `lazygit`, `shellcheck`, `nixfmt-rfc-style`, `curl`, `wget`, `unzip`, `tree`, `jq`, `yq-go`, `neofetch`, `sops`, `age`

#### `zsh.nix`
- VI keybindings
- Autosuggestions enabled
- History: 10,000 entries with dedup and sharing across sessions
- SSH agent init in `initContent`
- `.bashrc` generated for SSH sessions on non-NixOS systems

#### `git.nix`
- `user.name` from `hostSpec.gh_user`
- `user.email` from `hostSpec.email.gitHub`
- Default branch: `main`
- Pull: fast-forward only
- Push: current branch
- Core editor: `nvim`
- Auto-stash on rebase enabled

#### `direnv.nix`
- `nix-direnv` (better Nix integration than native direnv)
- Shell hooks for both bash and zsh

#### `starship.nix`
- Unicode arrows for ahead/behind/diverged (`⇡` `⇣` `⇕`)
- Git metrics with status display
- Container and Kubernetes modules disabled by default

#### `neovim.nix`

Comprehensive Neovim setup based on **EdenVim**:
- Python3 + Node.js provider support
- Config symlinked via `mkOutOfStoreSymlink` to `~/src/nixos/home/core/neovim` (live editing)
- Lazy.nvim plugin manager restored on activation

**LSP / Formatters / Linters installed as packages:**

| Language | Tools |
|----------|-------|
| Bash | `bashls`, `shfmt` |
| Lua | `lua-language-server`, `stylua` |
| Nix | `nixd`, `nil`, `nixfmt-rfc-style` |
| TypeScript/JS | `typescript-language-server`, `prettier` |
| Python | `basedpyright`, `ruff`, `isort`, `debugpy` |
| Terraform | `terraform-ls`, `tflint` |
| Docker | `dockerfile-language-server` |
| YAML | `yaml-language-server`, `yamlfmt`, `yamllint` |
| Tree-sitter | `tree-sitter` (parsers) |

#### `nixos.nix` (NixOS-specific home extras)
- `ssh-agent` systemd user service
- XDG MIME apps configuration
- Extra packages: `coreutils`, `keychain`

#### `darwin.nix` (macOS-specific home extras)
- Currently empty stub

---

### Optional Home Modules

#### `gnome/`
- **`ghostty.nix`**: Ghostty terminal, Catppuccin Latte theme, FiraCode 14pt
- **`stylix.nix`**: Stylix theme targets for GNOME
- **`dconf.nix`**: GNOME dconf settings
- **`cursor.nix`**: Cursor theme configuration

#### Single-module files
| Module | Contents |
|--------|----------|
| `browser.nix` | Firefox as default browser |
| `comms.nix` | Signal Desktop |
| `media.nix` | Spotify, VLC |
| `adb.nix` | Android tools, Heimdall |
| `calibre.nix` | Calibre e-book management |
| `freecad.nix` | FreeCAD |
| `moonlight.nix` | Moonlight game-streaming client |
| `wsl.nix` | `targets.genericLinux.enable` |

#### `ssh.nix` (client config)
- Default: no agent forwarding, no X11 forwarding, compression off, custom `ControlPath`
- `switch` host (192.168.10.2): legacy crypto algorithms (DH groups, RSA, 3DES) for old network gear
- `laconia` host: custom domain + non-standard port for home NAS

#### `sops.nix` (home-manager secrets)
- Age key file: `~/.config/sops/age/keys.txt`
- Per-host secrets file: `nix-secrets/sops/{hostName}.yaml`
- Secrets managed: SSH ed25519 private key

---

## Work Configuration Details (`home/ian.preston/work.nix`)

The work machine has several unique touches:

**Git configuration** — conditional per-org includes:
```
[includeIf "hasconfig:remote.*.url:git@github.com:databricks-eng/**"]
    path = ~/.gitconfig-work

[includeIf "hasconfig:remote.*.url:git@github.com:databricks-field-eng/**"]
    path = ~/.gitconfig-work
```
- SSH alias `git@github.com-emu` for EMU authentication (GitHub Enterprise Managed User)
- Work email populated from hostSpec

**Ghostty terminal** — explicit config file written by home-manager:
- Catppuccin Latte theme
- FiraCode Nerd Font at 14pt
- Written to `~/.config/ghostty/config` (not via Ghostty's Nix module, direct file)

**Zsh aliases:**
- `llm` — internal Databricks LLM tool
- `isaac` — internal Databricks tool

**Environment variables:**
- `I_DANGEROUSLY_OPT_IN_TO_UNSUPPORTED_ALPHA_TOOLS=true`
- `MCP_PRIVACY_SUMMARIZATION_ENABLED=true`

---

## Secrets Management Architecture

Uses **sops-nix** with **age** encryption:

1. Each NixOS host has an SSH host key (`/etc/ssh/ssh_host_ed25519_key`)
2. `ssh-to-age` derives an age key from the SSH key
3. sops files in nix-secrets are encrypted to that age key
4. At build/activation time, sops-nix decrypts secrets to `/run/secrets/`
5. Home-manager secrets use a per-user age key at `~/.config/sops/age/keys.txt`

Secrets stored:
- System password (hashed)
- User SSH private key (ed25519)
- Work username and hostname (for the work machine)
- Any other host-specific secrets in `nix-secrets/sops/{hostName}.yaml`

Bootstrap workflow (`scripts/helpers.sh`): colored output helpers, yes/no prompts, age key generation tooling.

---

## Theming

**Stylix** manages the global theme:
- Color scheme: **Catppuccin Latte** (light)
- Font: **Fira Code Nerd Font**
- Targets configured at both host (`hosts/common/optional/themes.nix`) and home (`home/core/neovim/` + `home/ipreston/gnome/stylix.nix`) levels

---

## Task Runner (`Taskfile.yaml`)

| Task | Command |
|------|---------|
| `rebuild` | `sudo nixos-rebuild switch --flake .` |
| `rebuild:{hostname}` | Target-specific NixOS rebuild |
| `build_darwin:{target}` | nix-darwin rebuild (`darwin-rebuild switch`) |
| `build_home:{target}` | Standalone home-manager rebuild |
| `update` | `nix flake update` |
| `update_dconf` | `scripts/dconf.sh` → writes `home/ipreston/gnome/dconf.nix` |
| `garbage_collect` | `nix-collect-garbage --delete-older-than 7d` |
| `iso` | Build the custom live ISO with zstd:3 compression |

---

## Developer Shell

`nix develop` from the repo root provides:
- `nixos-rebuild`
- `go-task`
- `dconf2nix`
- `alejandra` (Nix formatter)
- `nushell`
- `ssh-to-age`
- `pre-commit`

---

## Key Architectural Patterns & Design Decisions

### 1. `scanPaths` Auto-Import
Directories with optional modules use `scanPaths` to auto-discover `.nix` files. Adding a new module file to `hosts/common/optional/` or `home/core/` is enough — no explicit import list to update.

### 2. hostSpecs as Module Data
Host metadata lives in `hostSpecs/` as NixOS modules and is passed to every configuration via `specialArgs`. This cleanly separates static data from configuration logic.

### 3. `mkOutOfStoreSymlink` for Neovim
The Neovim config directory is symlinked into the Nix store path but points back to the working copy at `~/src/nixos/home/core/neovim`. This allows live editing of Lua plugin configs without rebuilding.

### 4. Platform Abstraction via `isDarwin`
Common patterns get a `hosts/common/core/nixos.nix` and `hosts/common/core/darwin.nix` pair. The `default.nix` imports the correct one based on the `isDarwin` flag. Same pattern in `home/core/`.

### 5. Private Secrets Repository
`nix-secrets` is a separate private git repository referenced as a flake input. This lets the configuration repo be public while secrets remain private and encrypted.

### 6. Declarative Disk Partitioning with disko
All NixOS hosts have their partition layout described in Nix. First boot can be automated with `disko --mode disko` before `nixos-install`.

### 7. Work Machine Isolation
The work machine uses:
- A separate username/hostname from secrets (not in the public repo)
- A separate git config include for work GitHub orgs
- EMU SSH key aliasing
- Separate email addresses
- Homebrew for work CLI tools (keeps them out of NixOS package management)

### 8. Impermanence-Ready Disk Layout
btrfs subvolumes (`@root`, `@nix`, `@swap`) follow the impermanence pattern, where `/nix` persists across wipes but `/` can be reset. Impermanence is not currently enabled but the disk layout supports it.

### 9. ISO with Age Stamp
The custom live ISO sets the shell prompt to include the ISO build time. This prevents using a stale ISO for recovery without knowing it's old.

---

## Notable Quirks and Specifics

- **RTX 5080 on stable drivers**: Uses `hardware.nvidia.package = config.boot.kernelPackages.nvidiaPackages.stable` — the 5080 is new enough that only the stable drivers (not the legacy stable) support it.
- **GTX 1060 PRIME offload**: The laptop runs on Intel iGPU normally, offloading to Nvidia only when needed via `DRI_PRIME=1`.
- **Chromebook GRUB**: Uses `grub.device = "nodev"` with `grub.efiInstallAsRemovable = true` and `efiSupport + cros_legacy` for dual-boot compatibility with ChromeOS firmware.
- **Printing security**: `cups-browsed` is explicitly disabled (it has had high-profile CVEs).
- **NAS mounts**: `laconia` NAS shares mounted via CIFS (`smbclient.nix`). The `switch` SSH host uses legacy crypto for what appears to be an old network switch that doesn't support modern algorithms.
- **Unstable overlay for OpenRGB**: `pkgs-unstable` is passed alongside main `pkgs` to get newer OpenRGB. The rest of the system uses stable.

---

## File Count Summary

- `hosts/common/optional/`: ~15 optional host modules
- `home/core/`: ~7 core home modules + neovim Lua config tree
- `home/ipreston/`: 5 host-specific home configs + optional modules
- `hostSpecs/`: 6 host specs (terra, luna, toshibachromebook, iso, work, penguin)
- `hosts/nixos/`: 4 NixOS host dirs
- `hosts/darwin/`: 1 darwin host dir
