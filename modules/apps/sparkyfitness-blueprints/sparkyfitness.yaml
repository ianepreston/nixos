---
version: 1
metadata:
  name: sparkyfitness
entries:
  - model: authentik_providers_oauth2.oauth2provider
    id: prov-sparkyfitness
    identifiers:
      name: sparkyfitness
    attrs:
      client_type: confidential
      # The !Env names come from myAuthentik.oidcApps' worker env file,
      # which derives them from the attribute name — so SPARKYFITNESS_*,
      # not the SPARKY_FITNESS_* names the app itself reads.
      client_id: !Env SPARKYFITNESS_OIDC_CLIENT_ID
      client_secret: !Env SPARKYFITNESS_OIDC_CLIENT_SECRET
      authentication_flow: !Find [authentik_flows.flow, [slug, default-authentication-flow]]
      authorization_flow: !Find [authentik_flows.flow, [slug, default-provider-authorization-implicit-consent]]
      invalidation_flow: !Find [authentik_flows.flow, [slug, default-provider-invalidation-flow]]
      property_mappings:
        - !Find [authentik_providers_oauth2.scopemapping, [managed, "goauthentik.io/providers/oauth2/scope-openid"]]
        - !Find [authentik_providers_oauth2.scopemapping, [managed, "goauthentik.io/providers/oauth2/scope-email"]]
        - !Find [authentik_providers_oauth2.scopemapping, [managed, "goauthentik.io/providers/oauth2/scope-profile"]]
      signing_key: !Find [authentik_crypto.certificatekeypair, [name, "authentik Self-signed Certificate"]]
      # authentik 2026.x added OAuth2Provider.grant_types (default empty); the
      # authorize view rejects flows whose grant is not listed. Required so a
      # provider created fresh on 2026.x is usable (see komga.yaml for detail).
      grant_types:
        - authorization_code
        - refresh_token
      redirect_uris:
        # Better Auth's SSO callback, built as
        # `${SPARKY_FITNESS_FRONTEND_URL}/api/auth/sso/callback/${provider_id}`
        # in SparkyFitnessServer/models/oidcProviderRepository.ts. The
        # final segment is SPARKY_FITNESS_OIDC_PROVIDER_SLUG, so this
        # URI and that env var have to move together.
        - matching_mode: strict
          url: https://sparkyfitness.@serverDomain@/api/auth/sso/callback/authentik
      access_code_validity: minutes=1
      access_token_validity: minutes=10
      refresh_token_validity: days=30
      sub_mode: hashed_user_id
      issuer_mode: per_provider
  - model: authentik_core.application
    id: app-sparkyfitness
    identifiers:
      slug: sparkyfitness
    attrs:
      name: SparkyFitness
      provider: !KeyOf prov-sparkyfitness
      group: Users
      open_in_new_tab: true
      meta_launch_url: https://sparkyfitness.@serverDomain@
      meta_icon: https://raw.githubusercontent.com/CodeWithCJ/SparkyFitness/main/SparkyFitnessFrontend/public/images/icons/icon-192x192.png
      policy_engine_mode: all
  - model: authentik_policies.policybinding
    identifiers:
      target: !KeyOf app-sparkyfitness
      order: 0
    attrs:
      group: !Find [authentik_core.group, [name, Users]]
      enabled: true
