version: 1
metadata:
  name: immich
entries:
  - model: authentik_providers_oauth2.oauth2provider
    id: prov-immich
    identifiers:
      name: immich
    attrs:
      client_type: confidential
      client_id: !Env IMMICH_OIDC_CLIENT_ID
      client_secret: !Env IMMICH_OIDC_CLIENT_SECRET
      authentication_flow: !Find [authentik_flows.flow, [slug, default-authentication-flow]]
      authorization_flow: !Find [authentik_flows.flow, [slug, default-provider-authorization-implicit-consent]]
      invalidation_flow: !Find [authentik_flows.flow, [slug, default-provider-invalidation-flow]]
      property_mappings:
        - !Find [authentik_providers_oauth2.scopemapping, [managed, "goauthentik.io/providers/oauth2/scope-openid"]]
        - !Find [authentik_providers_oauth2.scopemapping, [managed, "goauthentik.io/providers/oauth2/scope-email"]]
        - !Find [authentik_providers_oauth2.scopemapping, [managed, "goauthentik.io/providers/oauth2/scope-profile"]]
      signing_key: !Find [authentik_crypto.certificatekeypair, [name, "authentik Self-signed Certificate"]]
      # Web client lands on /auth/login after the IdP redirect; mobile
      # uses the app.immich:/ custom URL scheme. Both have to be
      # registered or the corresponding client breaks.
      redirect_uris:
        - matching_mode: strict
          url: https://immich.@serverDomain@/auth/login
        - matching_mode: strict
          url: https://immich.@serverDomain@/user-settings
        - matching_mode: strict
          url: app.immich:///oauth-callback
      access_code_validity: minutes=1
      access_token_validity: minutes=10
      refresh_token_validity: days=30
      sub_mode: hashed_user_id
      issuer_mode: per_provider

  - model: authentik_core.application
    id: app-immich
    identifiers:
      slug: immich
    attrs:
      name: Immich
      provider: !KeyOf prov-immich
      group: Home
      open_in_new_tab: true
      meta_launch_url: https://immich.@serverDomain@
      meta_icon: https://raw.githubusercontent.com/homarr-labs/dashboard-icons/main/png/immich.png
      # `all` fail-secure: if a second binding gets added later, both
      # must pass instead of either admitting the user.
      policy_engine_mode: all

  - model: authentik_policies.policybinding
    identifiers:
      target: !KeyOf app-immich
      order: 0
    attrs:
      group: !Find [authentik_core.group, [name, Home]]
      enabled: true
