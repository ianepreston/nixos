---
version: 1
metadata:
  name: manyfold
entries:
  # Migration cleanup: manyfold used to authenticate via
  # myAuthentik.forwardAuthApps (a proxy/forward-auth provider), and the
  # embedded outpost owned a ProxyProvider named "manyfold". The OIDC
  # migration (PR #426) dropped the forward-auth declaration but that only
  # removes the rendered forward-auth blueprint — it does not delete the
  # already-created DB object. Because a ProxyProvider is a subclass of
  # OAuth2Provider (Django multi-table inheritance: one row, same id), the
  # OAuth2Provider entry below matched the leftover proxy provider by name
  # and merged into it instead of creating a clean provider. authentik's
  # proxy save logic then re-derives redirect_uris from the external host
  # to the outpost callbacks (…/outpost.goauthentik.io/callback) on every
  # save — e.g. an authentik restart on nightly auto-upgrade — clobbering
  # the OIDC callback the entry below sets. The result is the
  # "mismatching redirect_uri" error against manyfold's real callback
  # (/users/auth/openid_connect/callback).
  #
  # Deleting the proxy provider deletes the whole MTI object (both rows);
  # the OAuth2Provider entry that follows then recreates a clean,
  # proxy-free provider whose redirect_uris stick. Ordered first so the
  # delete happens before the recreate within a single apply. Idempotent:
  # once the object is a plain OAuth2Provider this find-by-name matches
  # nothing and is a no-op, so it is safe to leave in place. (Application
  # .provider is on_delete=SET_NULL, so the application survives the
  # delete and the application entry below re-links it.)
  - model: authentik_providers_proxy.proxyprovider
    identifiers:
      name: manyfold
    state: absent

  # As of authentik 2025.10 the default email scope mapping returns
  # email_verified: false (security change, goauthentik/authentik#16205).
  # Manyfold's User.from_omniauth refuses to link an OIDC identity to an
  # existing account unless email_verified is true ("Email not verified"),
  # so use a custom email scope that hardcodes true — same pattern komga
  # needs. Safe here because all authentik users are admin-provisioned.
  - model: authentik_providers_oauth2.scopemapping
    id: scope-manyfold-email
    identifiers:
      name: "Manyfold email (verified)"
    attrs:
      scope_name: email
      description: "Email scope returning email_verified: true for Manyfold"
      expression: |
        return {
            "email": request.user.email,
            "email_verified": True,
        }

  - model: authentik_providers_oauth2.oauth2provider
    id: prov-manyfold
    identifiers:
      name: manyfold
    attrs:
      client_type: confidential
      client_id: !Env MANYFOLD_OIDC_CLIENT_ID
      client_secret: !Env MANYFOLD_OIDC_CLIENT_SECRET
      authentication_flow: !Find [authentik_flows.flow, [slug, default-authentication-flow]]
      authorization_flow: !Find [authentik_flows.flow, [slug, default-provider-authorization-implicit-consent]]
      invalidation_flow: !Find [authentik_flows.flow, [slug, default-provider-invalidation-flow]]
      property_mappings:
        - !Find [authentik_providers_oauth2.scopemapping, [managed, "goauthentik.io/providers/oauth2/scope-openid"]]
        - !KeyOf scope-manyfold-email
        - !Find [authentik_providers_oauth2.scopemapping, [managed, "goauthentik.io/providers/oauth2/scope-profile"]]
      signing_key: !Find [authentik_crypto.certificatekeypair, [name, "authentik Self-signed Certificate"]]
      # authentik 2026.x added OAuth2Provider.grant_types (default empty); the
      # authorize view rejects flows whose grant is not listed. Required so a
      # provider created fresh on 2026.x is usable (see komga.yaml for detail).
      grant_types:
        - authorization_code
        - refresh_token
      # Manyfold (omniauth_openid_connect) posts back to a fixed callback path
      # derived from PUBLIC_HOSTNAME; see manyfold docs "OIDC configuration".
      redirect_uris:
        - matching_mode: strict
          url: https://manyfold.@serverDomain@/users/auth/openid_connect/callback
      access_code_validity: minutes=1
      access_token_validity: minutes=10
      refresh_token_validity: days=30
      sub_mode: hashed_user_id
      issuer_mode: per_provider
  - model: authentik_core.application
    id: app-manyfold
    identifiers:
      slug: manyfold
    attrs:
      name: Manyfold
      provider: !KeyOf prov-manyfold
      group: Users
      open_in_new_tab: true
      meta_launch_url: https://manyfold.@serverDomain@
      meta_icon: https://raw.githubusercontent.com/homarr-labs/dashboard-icons/main/png/manyfold.png
      policy_engine_mode: all
  - model: authentik_policies.policybinding
    identifiers:
      target: !KeyOf app-manyfold
      order: 0
    attrs:
      group: !Find [authentik_core.group, [name, Users]]
      enabled: true
